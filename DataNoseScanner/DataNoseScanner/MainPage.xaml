<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DataNoseScanner"
             xmlns:forms="clr-namespace:Xamarin.Forms;assembly=Xamarin.Forms.CarouselView"
             x:Class="DataNoseScanner.MainPage"
             BackgroundColor="White">
    <NavigationPage.TitleView>
        <StackLayout Orientation="Horizontal">
            <Label Text="Datanose Scanner" FontSize="24" TextColor="white" VerticalTextAlignment="Center"/>
        </StackLayout>
    </NavigationPage.TitleView>
    <ContentPage.Content>
        <ScrollView>
        <StackLayout Orientation="Vertical" Padding="30" Spacing="40">

            <Image HorizontalOptions="Center">
                <Image.Source>
                    <OnPlatform x:TypeArguments="ImageSource">
                        <OnPlatform.iOS>
                            <FileImageSource File="datanose"/>
                        </OnPlatform.iOS>
                        <OnPlatform.Android>
                        <FileImageSource File="ic_launcher_round.png"/>
                        </OnPlatform.Android>
                    </OnPlatform>
                </Image.Source>
            </Image>
            <Image HorizontalOptions="Center">
                <Image.Source>
                    <OnPlatform x:TypeArguments="ImageSource">
                        <OnPlatform.iOS>
                            <FileImageSource File="uvalogo"/>
                        </OnPlatform.iOS>
                        <OnPlatform.Android>
                            <FileImageSource File="uvalogo.png"/>
                        </OnPlatform.Android>
                    </OnPlatform>
                </Image.Source>
            </Image>

            
                <forms:CarouselView x:Name="carouselInfo">
                     <forms:CarouselView.ItemTemplate>
                         <DataTemplate>
                            <Frame BackgroundColor="#F0F0F0" HasShadow="False">
                            <StackLayout>
                                    <Label Text="{Binding id}" x:Name="lblUvAnetID" HorizontalOptions="Center" HeightRequest="40"></Label>
                                    <Label Text="{Binding student}" x:Name="lblStudent" HorizontalOptions="Center" HeightRequest="40"></Label>
                                    <Label Text="{Binding programme}" x:Name="lblProgramme" HorizontalOptions="Center" HeightRequest="40"></Label>
                                    <Label Text="{Binding remarks}" x:Name="lblRemarks" HorizontalOptions="Center" HeightRequest="40"></Label>
                                </StackLayout>
                            </Frame>
                        </DataTemplate>
                     </forms:CarouselView.ItemTemplate>
                </forms:CarouselView>
               

                <Button Text="Scan Code" Clicked="btnScan_Clicked" TextColor="White"  
                    FontAttributes="Bold" FontSize="Large" HorizontalOptions="FillAndExpand"  
                    BackgroundColor="Black" Margin="0,0,0,20"/>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Text="Sign out" Clicked="btnSignout_Clicked" Grid.Row="0" Grid.Column="0"></Button>
                    <Button Text="Datanose" Clicked="btnDatanose_Clicked" Grid.Row="0" Grid.Column="1"></Button>
                </Grid>

            </StackLayout>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>
